---
const { caption, className = "" } = Astro.props;
---

<div class={`flex flex-col items-center flex-wrap gap-2 justify-center [&_img]:rounded-lg [&_img,p]:m-0 mb-10 text-center ${className}`}>
  <slot /> <!-- markdown image goes here -->

  {caption && (
    <p class="italic opacity-70 text-sm">{caption}</p>
  )}
</div>
